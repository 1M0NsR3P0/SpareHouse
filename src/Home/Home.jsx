import React from 'react';
import { Outlet } from 'react-router-dom';

const Home = () => {
    return (
        <div id='home'>
            all the pages will be here
            
        </div>
    );
};

export default Home;