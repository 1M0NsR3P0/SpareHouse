import React from 'react';
import { Outlet } from 'react-router-dom';

const Home = () => {
    return (
        <div>
            all the pages will be here
            
        </div>
    );
};

export default Home;